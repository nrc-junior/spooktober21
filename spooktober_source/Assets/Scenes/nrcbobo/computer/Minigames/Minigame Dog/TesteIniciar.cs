using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TesteIniciar : MonoBehaviour {
    void OnEnable() {
        Debug.Log("Script do Minigame ao abrir ?");
    }
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    void OnDisable() {
        Debug.Log("Script do Minigame ao Fechar");
    }
}
